<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controlsalud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- LoginActivity - Pantalla de inicio -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- MenuActivity -->
        <activity
            android:name=".MenuActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- EjerciciosActivity - CRUD 1 -->
        <activity
            android:name=".EjerciciosActivity"
            android:exported="false"
            android:label="@string/ejercicios_title" />

        <!-- AlimentosActivity - CRUD 2 -->
        <activity
            android:name=".AlimentosActivity"
            android:exported="false"
            android:label="@string/alimentos_title" />

        <!-- RegistrarEjercicioActivity - Maestro-Detalle -->
        <activity
            android:name=".RegistrarEjercicioActivity"
            android:exported="false"
            android:label="@string/registrar_ejercicio_title" />

        <!-- RegistrarAlimentoActivity - Maestro-Detalle -->
        <activity
            android:name=".RegistrarAlimentoActivity"
            android:exported="false"
            android:label="Registrar Mi Alimento" />

        <!-- ResumenActivity -->
        <activity
            android:name=".ResumenActivity"
            android:exported="false"
            android:label="Resumen General" />

        <!-- PerfilActivity -->
        <activity
            android:name=".PerfilActivity"
            android:exported="false"
            android:label="Mi Perfil" />

    </application>

</manifest>